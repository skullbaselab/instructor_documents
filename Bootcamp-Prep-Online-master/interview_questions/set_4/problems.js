// EASY
/*------------------------------------------------------------------------------
// isPerfectNum
//
// A perfect number is a number that is equal to the sum of its factors (excluding itself).
// For example, the factors of 6 are 1, 2, and 3. This means 6 is a perfect number
// because 1 + 2 + 3 = 6. Write a function `isPerfectNum(num)` that returns true if
// the given `num` is perfect and false if it is not.
//
// Examples:
//
// isPerfectNum(6); // => true
// isPerfectNum(8); // => false
// isPerfectNum(25); // => false
// isPerfectNum(28); // => true
// isPerfectNum(30); // => false
// isPerfectNum(496); // => true
------------------------------------------------------------------------------*/


// MEDIUM
/*------------------------------------------------------------------------------
// factorialArray
//
// Write a function `factorialArray(arr)` that takes in an array of numbers and returns
// a new array where every number is replaced with it's factorial. The factorial
// of a number is the product of all numbers less than or equal to the given number.
// For example, the factorial of 5 is 1 * 2 * 3 * 4 * 5 = 120.
//
// Example:
//
// factorialArray([5, 4, 6, 3]) // => [ 120, 24, 720, 6 ]
------------------------------------------------------------------------------*/


// HARD - PAIRING
/*------------------------------------------------------------------------------
// uniqueCharsInWords
//
// Write a function `uniqueCharsInWords(sentence)` that takes in a sentence and
// returns true if every word contains only unique characters and returns false
// otherwise. The same character can appear across different words, but within a
// single word no character should appear more than once.
//
//
// Examples:
//
// uniqueCharsInWords('this has repeated chars'); // => false
// uniqueCharsInWords('this has no duplicate chars'); // => true
// uniqueCharsInWords('javascript is fun'); // => false
// uniqueCharsInWords('coding is fun'); // => true
-------------------------------------------------------------------------------*
